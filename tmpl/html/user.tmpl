{{ define "user_info" }}
    <div id="attention" class="col s12">
        <div class="container">
            <p>Example</p>
        </div>
    </div>
    <div id="log" class="col s12">
        <div class="container">
            <ul>
                <h5>
                    <li>Example</li>
                </h5>
                <li>example</li>
            </ul>
        </div>
    </div>
    <div id="client" class="col s12">
        <div class="container">
            <p>Example</p>
        </div>
    </div>
    <div id="server" class="col s12">
        <div class="valign-wrapper" style="width:100%;height:100%;position: absolute;">
            <div class="valign" style="width:100%;">
                <div class="container">
                    <div class="row">
                        <div class="col card s12 l6 offset-l3">
                            <div class="card-content">
                                <span id="title" class="card-title">我的信息</span>
                                <div class="input-field">
                                    <i class="material-icons prefix">account_circle</i>
                                    <input id="uid" value="{{.uid}}" type="text" readonly>
                                    <label for="uid">UID</label>
                                </div>
                                <div class="input-field">
                                    <i class="material-icons prefix">email</i>
                                    <input id="email" value="{{.email}}" type="text" readonly>
                                    <label for="email">Email</label>
                                </div>
                                <div class="input-field">
                                    <i class="material-icons prefix">vpn_key</i>
                                    <input id="uuid" value="{{.uuid}}" type="text" readonly>
                                    <label for="uuid">UUID</label>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        <div>
    </div>
{{end}}